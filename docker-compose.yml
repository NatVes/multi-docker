services:
  client:
    image: "nataliaveselova/multi-client"
    mem_limit: 128m
    hostname: client
  server:
    image: "nataliaveselova/multi-server"
    mem_limit: 128m
    hostname: api
    extra_hosts:
      - "host.docker.internal:host-gateway"
    environment:
      - REDIS_HOST=$REDIS_HOST
      - REDIS_PORT=$REDIS_PORT
      - PGUSER=$PGUSER
      - PGHOST=$PGHOST
      - PGDATABASE=$PGDATABASE
      - PGPASSWORD=$PGPASSWORD
      - PGPORT=$PGPORT
  worker:
    image: "nataliaveselova/multi-worker"
    mem_limit: 128m
    hostname: worker
    extra_hosts:
      - "host.docker.internal:host-gateway"
    environment:
      - REDIS_HOST=$REDIS_HOST
      - REDIS_PORT=$REDIS_PORT
  nginx:
    image: "nataliaveselova/multi-nginx"
    mem_limit: 128m
    hostname: nginx
    ports:
      - "80:80"